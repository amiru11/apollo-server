const _interface = `
  interface User {
    id: Int!
    username: String!
    nickname: String
    isAdmin: Boolean!
    deleted: Boolean!
    createdAt: String
    updatedAt: String
  }
`;

export default { _interface };
